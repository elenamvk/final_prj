i create the offer, the offer can OR not call the true inside the method offered

if it call true then the request is done, and the offer cannot be deleted

if it calls false then the request is still pending, and the offer can be deleted

at the moment the method gets called whenever somebody makes an offer at all...

